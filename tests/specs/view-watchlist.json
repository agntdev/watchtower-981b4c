[
  {
    "name": "View watchlist when empty",
    "steps": [
      {
        "send": { "callback": "view_watchlist", "userId": 10 },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No coins on your watchlist yet — tap ➕ Add Bitcoin to start."
            }
          }
        ]
      }
    ]
  }
]
